 
Bootstrap: docker
From: python:3.9-slim

%files
req.out /tmp/req.out

%post
apt-get update && apt-get install -y --no-install-recommends \
    build-essential \
    && rm -rf /var/lib/apt/lists/*

pip install -r /tmp/req.out